# API Key de OpenAI (requerida para modo openai)
OPENAI_API_KEY=

# Modelo a usar — gpt-4.1-mini es el default recomendado
OPENAI_MODEL=gpt-4.1-mini

# Directorio de datos del runtime
AGENTIC_DATA_DIR=./runtime_data

# Modo de operacion: 'mock' (sin API) u 'openai'
AGENTIC_DEFAULT_MODE=mock
